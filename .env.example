# Copy this file to .env.local and paste your real key there.
# .env.local is git-ignored, so the real key never reaches the repository.
# The key is read ONLY by server-side code (used in Stage 4 - Gemini screenshot analysis).
GEMINI_API_KEY=your_google_ai_studio_key_here
